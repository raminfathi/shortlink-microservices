# --- Monitoring (Grafana) ---
# Default password for Grafana admin user
# IMPORTANT: Change this in production!
GRAFANA_PASSWORD=admin

# --- Database Credentials (PostgreSQL) ---
# These credentials are used by both the Postgres service container and the Django Auth Service
POSTGRES_DB=shortlink_db
POSTGRES_USER=postgres
# In production, use a strong password generated by a secrets manager
POSTGRES_PASSWORD=change_me_in_prod
POSTGRES_HOST=postgres
POSTGRES_PORT=5432

# --- Redis Configuration ---
# Hostname of the Redis Stack container (service name in docker-compose)
REDIS_HOST=redis-stack

# --- Django Security ---
# Secret key for Django cryptographic signing (Sessions, CSRF, etc.)
# IN PRODUCTION: Generate a new random string (e.g. using `openssl rand -base64 32`)
SECRET_KEY=django-insecure-change-this-random-string-for-dev

# --- Shared Security (JWT) ---
# Secret key shared between Auth Service (to sign tokens) and Core API (to verify tokens)
# This ensures that Core API can validate tokens issued by Auth Service without DB access.
# IN PRODUCTION: Use a strong, long random string.
JWT_SECRET_KEY=super-secret-key-change-me-in-production